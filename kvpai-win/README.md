# KVPAI — WINDOWS

Signed MSIX, installed/updated via Windows App Installer. To install, run `Install-KVPAI.cmd`.

The **Android** build (APK) is the separate [`kvpai-android/`](../kvpai-android/) folder.

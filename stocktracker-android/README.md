# KVP Stock Tracker — ANDROID

This folder holds the **Android** build only — `kvp-stock-tracker-arm64.apk`
plus its in-app update manifest (`latest.json`).

The **Windows** build is the separate [`stocktracker-win/`](../stocktracker-win/)
folder (a signed MSIX).
